
let nomeheroi = "superman"
let XP = 500
let nivel = ""


if (XP <=1000) {
    nivel = "Ferro"
}

if (XP >=1001 && XP <=2000 ) {
    nivel = "Bronze"
}


//saída
console.log("O Herói de nome" + nomeheroi + "está no nível de" + nivel + ".")